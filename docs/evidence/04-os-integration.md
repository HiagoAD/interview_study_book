# Evidence for chapter 04: Lifecycle, permissions, links, notifications, and sign-in

What Phase 22 checked the chapter’s claims against, and where the outline was wrong. [PLAN.md](../PLAN.md), under “Decisions: evidence”, says what counts as evidence.

The reference Editor is Unity 6000.3.11f1. Editor paths below are relative to that installation. Export paths are relative to the separate PlatformLayerProbe project. No chapter content was read or written. The managed and native compile checks passed; no lifecycle or delivery behavior is presented as a completed device run.

## Checked, and against what


### os-lifecycle

- lifecycle-01. Android activities move through created, started, resumed, paused, stopped and destroyed states. Without other active components, process kill likelihood rises from resumed to visible paused, stopped background, then empty: confirmed. Source: https://developer.android.com/guide/components/activities/activity-lifecycle.
- lifecycle-02. Cached Android processes can be killed at any time; onDestroy is not guaranteed. A final Unity callback cannot be a persistence requirement: confirmed. Source: https://developer.android.com/guide/components/activities/process-lifecycle.
- lifecycle-03. UnityPlayerActivity passes savedInstanceState to its superclass and constructs a new UnityPlayerForActivityOrService; it does not pass the Bundle into that player constructor: narrowed to Do not say Unity discards the Android Bundle or always passes null. The export does not show automatic restoration of managed gameplay state. Source: probe export `Exports/Android-Activity/unityLibrary/src/main/java/com/unity3d/player/UnityPlayerActivity.java`, line 33.
- lifecycle-04. UnityPlayerGameActivity also passes savedInstanceState to its superclass and constructs UnityPlayerForGameActivity in onCreateSurfaceView: narrowed to Android superclass restoration and Unity gameplay restoration are distinct; the exported class does not itself restore a managed session. Source: probe export `Exports/Android-GameActivity/unityLibrary/src/main/java/com/unity3d/player/UnityPlayerGameActivity.java`, line 46.
- lifecycle-05. The app-based lifecycle includes inactive, active and background transitions, followed by suspension or termination; scenes instead have independent lifecycles and can be disconnected to an unattached state: narrowed to Not running and suspended are conceptual process conditions, not two extra UIApplication.State enum cases; distinguish scene state from process state. Source: https://developer.apple.com/documentation/uikit/managing-your-app-s-life-cycle.
- lifecycle-06. An iOS app suspended in the background receives no CPU time: confirmed. Source: https://developer.apple.com/videos/play/wwdc2025/227/.
- lifecycle-07. A suspended iOS app has no guaranteed final applicationWillTerminate callback. Persist before suspension: narrowed to Apple describes this termination callback for executing background apps; do not promise it for suspended process removal. Source: https://developer.apple.com/documentation/uikit/uiapplicationdelegate/applicationwillterminate(_:).
- lifecycle-08. Apple gives applicationDidEnterBackground approximately five seconds to return; scene-based apps use sceneDidEnterBackground instead: confirmed. Source: https://developer.apple.com/documentation/uikit/uiapplicationdelegate/applicationdidenterbackground(_:).
- lifecycle-09. beginBackgroundTask requests finite additional execution time; request early, pair it with endBackgroundTask, and handle expiration. It is not indefinite background execution: confirmed. Source: https://developer.apple.com/documentation/uikit/uiapplication/beginbackgroundtask(expirationhandler:).
- lifecycle-10. OnApplicationPause reports paused or running state and sends an initial false notification after Awake: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/MonoBehaviour.OnApplicationPause.html.
- lifecycle-11. OnApplicationFocus reports focus changes; on Android opening the soft keyboard can report false. Focus loss alone does not establish that the app entered the background: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/MonoBehaviour.OnApplicationFocus.html.
- lifecycle-12. OnApplicationQuit is unreliable for mobile persistence, including iOS suspension and later OS termination: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/MonoBehaviour.OnApplicationQuit.html.
- lifecycle-13. Application.lowMemory is a foreground low-memory warning, not guaranteed advance notice of process termination. Release noncritical resources and persist transient state: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application-lowMemory.html.
- lifecycle-14. Application.memoryUsageChanged exists in Unity 6000.3; Android supports Medium, High and Critical, while iOS supports Critical: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application-memoryUsageChanged.html.
- lifecycle-15. Application.focusChanged provides focus notifications to code that does not use MonoBehaviour callbacks: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application-focusChanged.html.
- lifecycle-16. Application.quitting is not raised for forced termination or crashes and must not be treated as guaranteed cleanup: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application-quitting.html.
- lifecycle-17. Unity 6000.3 documents Enable Custom Background Behaviors, not the older Behavior in Background dropdown with Suspend, Exit and Custom values: contradicted: Use the current checkbox and explain native background execution separately. The current manual also lists Render Extra Frame on Pause. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/class-PlayerSettingsiOS.html.
- lifecycle-18. UnityScene forwards resign-active and background events to UnityAppController, and foreground entry initializes Unity with the window scene before forwarding: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UI/UnityScene.mm`, line 21.
- lifecycle-19. For a ready iOS player, resign-active clears focus, calls UnityDisplayLinkCallback, then pauses under the background-policy conditions; the optional snapshot path schedules OnApplicationPause with UnityWillPause and repaints first. Become-active conditionally calls UnityWillResume and UnityPause(0) before restoring focus: narrowed to These are native call orders under explicit guards, not measured managed callback order; lines 607-648 and 524-548 contain the paths. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, line 615.
- lifecycle-20. Android UnityPlayer chooses pause/resume versus stop/start behavior using MultiWindowSupport and handles window focus through a separate path. Cross-platform managed focus/pause ordering remains unverified: unverified. Source: `PlaybackEngines/AndroidPlayer/Variations/il2cpp/Release/Classes/classes.jar`, `javap -p -c com.unity3d.player.UnityPlayer`, methods `onPause`, `onResume`, `onStop`, `onStart`, `windowFocusChanged` and `UnitySendMessage` as applicable.
- lifecycle-21. UnitySendMessage is asynchronous, but the cited docs and Java wrapper do not establish whether a paused player retains, drops or bursts messages; unloaded native libraries are an explicit Android drop condition: narrowed to A dedicated paused-player run is needed before claiming that native messages queue and arrive in a burst on resume. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/ios-native-plugin-call-back.html.
- lifecycle-21. UnitySendMessage is asynchronous, but the cited docs and Java wrapper do not establish whether a paused player retains, drops or bursts messages; unloaded native libraries are an explicit Android drop condition: narrowed to The visible Java path proves this drop condition, not the behavior of messages sent while the native player is paused. Source: `PlaybackEngines/AndroidPlayer/Variations/il2cpp/Release/Classes/classes.jar`, `javap -p -c com.unity3d.player.UnityPlayer`, methods `onPause`, `onResume`, `onStop`, `onStart`, `windowFocusChanged` and `UnitySendMessage` as applicable.
- lifecycle-22. Pause handling should save pending work and quiet timers; resume should revalidate permissions, credentials, purchases and push registration as application policy. A guaranteed analytics flush, universal socket loss, or universal callback burst is unsupported: narrowed to Treat flushing as best effort and reconciliation as an application design rule; OS background execution does not guarantee network completion. Source: https://developer.apple.com/documentation/uikit/preparing-your-ui-to-run-in-the-background.
- lifecycle-23. Neither specified CoreModule nor AndroidJNIModule XML documents savedInstanceState, onSaveInstanceState or saved instance state. This scoped search does not establish that every native or package restoration API is absent: narrowed to No documented managed saved-Bundle bridge was found in the two requested XML files; explicitly persist gameplay and pending operations. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.CoreModule.xml` and `UnityEngine.AndroidJNIModule.xml`, complete text search.

- Detailed iOS trace: confirmed at the native-source level. `UnityScene.mm` forwards become-active at line 7, resign-active at line 16, foreground entry at line 25 and background entry at line 37. Foreground entry calls `initUnityWithScene:` before forwarding. In `UnityAppController.mm`, ordinary iOS resign-active clears focus at line 615, ends the display-link frame at line 619, records the prior pause state at line 621, and, unless custom background handling or another guard intervenes, pauses at line 647. The optional snapshot branch calls `UnityWillPause` at line 637 and repaints at line 638. Background entry pauses the display link at line 490 and cancels touches at line 491; the explicit focus/pause block at lines 493 to 499 is guarded for visionOS. Become-active calls `UnityWillResume` at line 535 and `UnityPause(0)` at line 536 before restoring focus at line 548. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UI/UnityScene.mm`, line 7, and `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, lines 486, 524 and 607.
- Detailed Android trace: confirmed as separate callback paths, with managed ordering still unverified. The Activity export invokes the superclass before `mUnityPlayer.onPause`, `onResume` and `windowFocusChanged`; the GameActivity export invokes the Unity methods before the corresponding superclass calls. `UnityPlayer` bytecode sends pause/resume through `setupUnityToBePaused` and `setupUnityToBeResumed`, selecting visibility callbacks in multi-window mode. `windowFocusChanged` separately invokes `saveFocusState`, `handleFocus` and conditionally `resumeUnity`. The native implementation and OS callback scheduling are beyond this bytecode, so these paths do not establish one universal C# sequence. Source: probe `Exports/Android-Activity/unityLibrary/src/main/java/com/unity3d/player/UnityPlayerActivity.java`, lines 78, 91, 98 and 137; `Exports/Android-GameActivity/unityLibrary/src/main/java/com/unity3d/player/UnityPlayerGameActivity.java`, lines 97, 112, 120 and 135; Editor `PlaybackEngines/AndroidPlayer/Variations/il2cpp/Release/Classes/classes.jar`, `UnityPlayer` methods named above.
- Restart after process death: narrowed to a fresh process and managed heap, not a claim that Android throws away all saved UI state. The Activity constructs its player at line 38; GameActivity does so in `onCreateSurfaceView` at line 78. Neither passes the saved Bundle to that constructor. The two requested XML files have no saved-instance-state entry. A custom native bridge could expose additional state; this inspection does not rule one out. Source: the two export classes above and https://developer.android.com/guide/components/activities/process-lifecycle.


### os-permissions

- permissions-01. Android distinguishes install-time permissions, including normal and signature permissions, from runtime dangerous permissions and special access. Declare required permissions in the merged manifest before requesting runtime access: confirmed. Source: https://developer.android.com/guide/topics/permissions/overview.
- permissions-02. Starting with Android 11, repeated denial during an installation suppresses further permission prompts; denial twice is represented by USER_FIXED: confirmed. Source: https://developer.android.com/about/versions/11/privacy/permissions.
- permissions-03. Check permission before each protected operation, ask in context, show rationale when shouldShowRequestPermissionRationale says to, and preserve optional-feature alternatives after denial. Rechecking on resume does not replace checking at use: confirmed. Source: https://developer.android.com/training/permissions/requesting.
- permissions-04. An explicit user action can open Android app details settings with ACTION_APPLICATION_DETAILS_SETTINGS and a package: URI. Do not repeatedly pressure the user after denial: confirmed. Source: https://developer.android.com/reference/android/provider/Settings.
- permissions-05. Permission defines Camera, CoarseLocation, FineLocation, Microphone, ExternalStorageRead and ExternalStorageWrite; its own documented methods are HasUserAuthorizedPermission, ShouldShowRequestPermissionRationale, and singular/plural RequestUserPermission(s) overloads with or without PermissionCallbacks. These APIs accept permission strings not represented by a predefined constant: confirmed. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.AndroidJNIModule.xml`, line 1280.
- permissions-06. PermissionCallbacks exposes PermissionGranted, PermissionDenied, PermissionDeniedAndDontAskAgain and PermissionRequestDismissed, plus a parameterless constructor and Invoke(string, IntPtr) returning IntPtr. Its permission XML documents the events and type, not the constructor or override: confirmed. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.AndroidJNIModule.xml`, line 1312.
- permissions-06. PermissionCallbacks exposes PermissionGranted, PermissionDenied, PermissionDeniedAndDontAskAgain and PermissionRequestDismissed, plus a parameterless constructor and Invoke(string, IntPtr) returning IntPtr. Its permission XML documents the events and type, not the constructor or override: confirmed. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.AndroidJNIModule.dll`, `monodis` declarations and custom attributes for `PermissionCallbacks`.
- permissions-07. PermissionDeniedAndDontAskAgain includes repeated denial in its 6.3 reference, but the shipped assembly marks it obsolete and unreliable: narrowed to The event exists, but new code should use PermissionDenied and a fresh rationale/status query instead of treating it as a reliable permanent-denial signal. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Android.PermissionCallbacks.PermissionDeniedAndDontAskAgain.html.
- permissions-07. PermissionDeniedAndDontAskAgain includes repeated denial in its 6.3 reference, but the shipped assembly marks it obsolete and unreliable: narrowed to The event exists, but new code should use PermissionDenied and a fresh rationale/status query instead of treating it as a reliable permanent-denial signal. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.AndroidJNIModule.dll`, `monodis` declarations and custom attributes for `PermissionCallbacks`.
- permissions-08. Unity still automatically requests certain permissions at runtime, normally on first feature use, not every manifest permission indiscriminately at startup. unityplayer.SkipPermissionsDialog disables Unity automatic requests; request manually afterward: contradicted: The 6.3 manual describes first-use requests for location, camera and microphone. A library-added manifest declaration is a separate build/merge concern. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/android-permissions-in-unity.html.
- permissions-09. Android 13, API level 33, introduces POST_NOTIFICATIONS for non-exempt notifications; the permission is declared in the manifest and requested at runtime: confirmed. Source: https://developer.android.com/develop/ui/compose/notifications/notification-permission.
- permissions-10. An iOS capture app must include the appropriate camera or microphone purpose key before requesting authorization or using the capture device; Apple says the system otherwise terminates the app: narrowed to Use the camera/microphone case for the exercise. Do not claim every permission-like API terminates identically when configuration is absent. Source: https://developer.apple.com/documentation/avfoundation/requesting-authorization-to-capture-and-save-media.
- permissions-11. Location Allow Once grants temporary When In Use authorization that expires when the app is no longer in use, returning to Not Determined: confirmed. Source: https://developer.apple.com/documentation/corelocation/cllocationmanager/requestwheninuseauthorization().
- permissions-12. Explicit notification authorization records the first answer and subsequent calls do not prompt again; provisional authorization delivers a quiet trial without that initial prompt. Check authorization again before scheduling: narrowed to Prompt-once is API-specific; temporary location access and current ATT rules have different behavior. Source: https://developer.apple.com/documentation/usernotifications/asking-permission-to-use-notifications.
- permissions-13. App Tracking Transparency requires NSUserTrackingUsageDescription; using the framework without the key crashes the app: confirmed. Source: https://developer.apple.com/documentation/bundleresources/information-property-list/nsusertrackingusagedescription.
- permissions-14. Do not describe every iOS permission prompt as permanently one-time: current ATT documentation allows another request after a year in the European Union, subject to its stated conditions: contradicted: Scope one-time language to the specific authorization API. A dismissed, undecided ATT request may also be requested again. Source: https://developer.apple.com/documentation/apptrackingtransparency/attrackingmanager/requesttrackingauthorization(completionhandler:).
- permissions-15. UIApplicationOpenSettingsURLString opens the app settings page through UIApplication.open; authorization must be checked again after returning: confirmed. Source: https://developer.apple.com/documentation/uikit/uiapplication/opensettingsurlstring.
- permissions-16. Application.RequestUserAuthorization requests iOS camera or microphone access and returns an AsyncOperation; use HasUserAuthorization to inspect the result. It is not the iOS notifications or ATT API: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application.RequestUserAuthorization.html.
- permissions-17. The iOS Player Settings expose Camera Usage Description, Microphone Usage Description and Location Usage Description; provide purpose text before exercising the corresponding protected APIs: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/class-PlayerSettingsiOS.html.

- Complete declared permission API inventory: confirmed, with doc text extracted separately for the writer. `Permission.Camera` describes camera access, `CoarseLocation` coarse location, `FineLocation` high-precision location, `Microphone` microphone access, and `ExternalStorageRead` and `ExternalStorageWrite` external-storage access. The presence of the storage constants is not a recommendation to use them on every current Android release. `HasUserAuthorizedPermission(string)` checks authorization. `ShouldShowRequestPermissionRationale(string)` returns the corresponding Android rationale decision. `RequestUserPermission(string)` and `RequestUserPermission(string, PermissionCallbacks)` request one permission; `RequestUserPermissions(string[])` and `RequestUserPermissions(string[], PermissionCallbacks)` request several. The callback-bearing overloads accept the object that receives the result. Unknown permission constants can be supplied as Android permission strings. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.AndroidJNIModule.xml`, lines 1209 to 1287.
- Complete declared callback inventory: confirmed. `PermissionGranted` reports Allow, `PermissionDenied` reports Deny, `PermissionDeniedAndDontAskAgain` documents explicit permanent denial, repeated denial on newer Android, or an OS decision not to request again, and `PermissionRequestDismissed` reports dismissal without choosing an option. Their payload is the permission name. There is also a public parameterless constructor and the JNI-facing `Invoke(string, IntPtr)` override returning `IntPtr`, with no corresponding permission-specific XML summary. Inherited `AndroidJavaProxy` and `Object` members are not new permission APIs. Source: `Unity.app/Contents/Resources/Scripting/Managed/UnityEngine/UnityEngine.AndroidJNIModule.xml`, lines 1289 to 1317, and the adjacent DLL’s `PermissionCallbacks` declarations.
- Rationale and Settings: narrowed. A false rationale result alone is not proof of permanent denial; it also occurs before a first request. Keep permission status, request history and the optional-feature alternative distinct. Android opens the app details page using `new Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS, Uri.parse("package:" + packageName))`; iOS uses `UIApplicationOpenSettingsURLString`. Settings navigation should be an explicit user choice, not repeated pressure. Source: https://developer.android.com/training/permissions/requesting and https://developer.android.com/reference/android/provider/Settings and https://developer.apple.com/documentation/uikit/uiapplication/opensettingsurlstring.
- Purpose fields: confirmed. The Unity iOS fields are Camera Usage Description, Microphone Usage Description and Location Usage Description. Camera and microphone requests require `NSCameraUsageDescription` and `NSMicrophoneUsageDescription`; location uses the location authorization configuration appropriate to the requested service. The reference describes `RequestUserAuthorization` only for iOS camera/microphone and Web camera, so it must not be presented as a universal permission wrapper. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/class-PlayerSettingsiOS.html and https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application.RequestUserAuthorization.html.


### os-deep-links

- links-01. iOS custom schemes are not exclusive: when multiple apps register a scheme, the selected app is undefined: confirmed. Source: https://developer.apple.com/documentation/xcode/defining-a-custom-url-scheme-for-your-app.
- links-02. Android custom schemes can collide; an unverified web intent generally opens the default browser on Android 12, API level 31, and later: confirmed. Source: https://developer.android.com/training/app-links/create-deeplinks.
- links-03. android:autoVerify="true" requests App Links domain verification; use adb shell pm verify-app-links --re-verify PACKAGE_NAME and adb shell pm get-app-links PACKAGE_NAME to trigger and inspect verification: confirmed. Source: https://developer.android.com/training/app-links/verify-applinks.
- links-04. Publish HTTPS /.well-known/assetlinks.json with relation delegate_permission/common.handle_all_urls, namespace android_app, package_name and sha256_cert_fingerprints. With Play App Signing, use the delivered app signing certificate and the Play Console App signing JSON, not an upload-key fingerprint: confirmed. Source: https://developer.android.com/training/app-links/configure-assetlinks.
- links-05. Universal Links require an applinks: associated-domain entitlement and an HTTPS .well-known/apple-app-site-association file without a filename extension or redirects. Its applinks details use appIDs and components; app identifiers include the application identifier prefix and bundle ID: confirmed. Source: https://developer.apple.com/documentation/xcode/supporting-associated-domains.
- links-06. Missing, inaccessible, redirected or malformed AASA data can prevent Universal Links approval and leave the URL opening in a browser. User choices and same-domain browser navigation also affect routing: narrowed to A valid association does not force every HTTPS navigation into the app; test a tap from another app, not just a browser address-bar entry. Source: https://developer.apple.com/documentation/technotes/tn3155-debugging-universal-links.
- links-07. The Unity Android deep-link setup uses manifest intent filters; route both cold launch and running-app links through the Unity deep-link APIs: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/deep-linking-android.html.
- links-08. Unity separates iOS Supported URL schemes from Universal Links; Universal Links need the Apple associated-domain setup: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/deep-linking-ios.html.
- links-09. Application.absoluteURL exposes the deep-link URL that launched or activated the Android or iOS app. Inspect it during startup: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application-absoluteURL.html.
- links-10. Application.deepLinkActivated handles activation by a link while the app is running and updates Application.absoluteURL: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/ScriptReference/Application-deepLinkActivated.html.
- links-11. UnityAppController sets the absolute URL in application:openURL:options: and posts kUnityOnOpenURL to listeners: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, line 343.
- links-12. UnityAppController reads userActivity.webpageURL and sets the absolute URL in application:continueUserActivity:restorationHandler:: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, line 349.
- links-13. The launch-options URL path is in willFinishLaunchingWithOptions, not didFinishLaunchingWithOptions: it extracts a URL, initializes without graphics, then calls UnitySetAbsoluteURL: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, line 358.
- links-14. The shipped iOS template opts into scenes with UnityScene as UISceneDelegateClassName and disables multiple scenes: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Info.plist`, line 39.
- links-15. The shipped UnityScene implements only four lifecycle methods; it has no willConnect, openURLContexts or continueUserActivity implementation. This leaves a scene URL-forwarding gap to resolve in the separately scheduled run: narrowed to The inspected source lacks these handlers; actual cold and warm link delivery is being tested separately, so do not infer a successful runtime path from app-delegate methods alone. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UI/UnityScene.mm`, line 37.
- links-17. Cold custom-scheme links are available through UISceneConnectionOptions.urlContexts: confirmed. Source: https://developer.apple.com/documentation/uikit/uiscene/connectionoptions/urlcontexts.
- links-18. Cold Universal Links use the scene connection options userActivities; an activity can contain the originating webpage URL: confirmed. Source: https://developer.apple.com/documentation/uikit/uiscene/connectionoptions/useractivities.
- links-19. A running scene receives custom-scheme URLs in scene(_:openURLContexts:): confirmed. Source: https://developer.apple.com/documentation/uikit/uiscenedelegate/scene(_:openurlcontexts:).
- links-20. A running scene receives Universal Link continuation through scene(_:continue:): confirmed. Source: https://developer.apple.com/documentation/uikit/uiscenedelegate/scene(_:continue:).
- links-21. When scenes are enabled, app-delegate launchOptions is nil; the launch-options URL path cannot substitute for scene connection handling: confirmed. Source: https://developer.apple.com/documentation/uikit/uiapplicationdelegate/application(_:didfinishlaunchingwithoptions:).
- links-22. TN3187 says UIScene lifecycle adoption becomes required in the next major release after iOS 26 when building with the latest SDK; without it the app will not launch. Multiple-scene support remains optional: narrowed to Use Apple’s conditional SDK/release wording; do not turn it into an unsupported calendar deadline. Source: https://developer.apple.com/documentation/technotes/tn3187-migrating-to-the-uikit-scene-based-life-cycle.
- links-23. Treat every link as untrusted input: allow-list and validate routes and parameters, authorize sensitive operations against trusted state, and queue application routing until ready. Idempotency and duplicate handling are application policies, not exactly-once OS guarantees: narrowed to A verified domain association identifies the intended handler; it does not authenticate the sender, grant rewards or validate parameter claims. Source: https://developer.android.com/privacy-and-security/risks/unsafe-use-of-deeplinks.
- links-24. Send a test link with adb shell am start -W -a android.intent.action.VIEW -d <URI> <PACKAGE>; repeat at cold start and while running. Package-targeted delivery alone does not prove App Links verification: confirmed. Source: https://developer.android.com/training/app-links/create-deeplinks.
- links-25. The requested simctl help openurl check could not complete because CoreSimulatorService was unavailable in this sandbox. The command syntax still needs a successful tool-help receipt: unverified. Source: `xcrun simctl help openurl`, unsuccessful sandbox invocation; no runtime behavior established.

- App Links file and verification commands: confirmed. The Digital Asset Links file is an array with a `relation` list containing `delegate_permission/common.handle_all_urls` and a `target` with `namespace: android_app`, `package_name` and `sha256_cert_fingerprints`. Publish it directly at `https://domain.name/.well-known/assetlinks.json` without redirects. With Play App Signing, the documentation directs the developer to Release > Setup > App signing for the correct JSON snippet. Inspect both `adb shell pm get-app-links PACKAGE_NAME` and user selection with `adb shell pm get-app-links --user cur PACKAGE_NAME`; trigger a new check with `adb shell pm verify-app-links --re-verify PACKAGE_NAME`. A package-targeted `am start` exercise tests intent delivery, not the domain’s trust state. Source: https://developer.android.com/training/app-links/configure-assetlinks and https://developer.android.com/training/app-links/verify-applinks.
- Universal Links file: confirmed. Use `applinks:domain.name` in the Associated Domains capability and an extensionless `apple-app-site-association` file in the domain’s `.well-known` directory. The current `applinks.details` format lists `appIDs` and `components`; each app identifier uses the application identifier prefix and bundle identifier. Apple also documents the legacy `appID` and `paths` format and warns against mixing the two. A missing or invalid association can leave links in the browser, as can a user choice or same-domain navigation. Source: https://developer.apple.com/documentation/xcode/supporting-associated-domains and https://developer.apple.com/documentation/technotes/tn3155-debugging-universal-links.
- Scene entry points: confirmed in Apple documentation, incomplete in the inspected UnityScene implementation. Cold custom URLs use `scene(_:willConnectTo:options:)` and `connectionOptions.urlContexts`; cold Universal Links use `connectionOptions.userActivities`. Warm custom URLs use `scene(_:openURLContexts:)`; warm Universal Links use `scene(_:continue:)`. App-delegate launch options are nil when scenes are supported, including the corresponding launch path documented for `willFinishLaunchingWithOptions`. Unity’s `extractURLFromLaunchOptions:` itself checks the direct launch URL and then webpage activities; the source path exists, but scene delivery must reach it or another equivalent bridge. Source: https://developer.apple.com/documentation/xcode/defining-a-custom-url-scheme-for-your-app and https://developer.apple.com/documentation/xcode/supporting-universal-links-in-your-app and https://developer.apple.com/documentation/uikit/uiscenedelegate/scene(_:willconnectto:options:) and `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, lines 355 and 368.


### os-notifications

- notifications-01. Mobile Notifications 2.5.0 is the released package listed for Unity Editor 6000.3: confirmed. Source: https://docs.unity3d.com/6000.3/Documentation/Manual/com.unity.mobile.notifications.html.
- notifications-02. Mobile Notifications schedules local one-time and repeating notifications on Android and iOS; it also exposes APNs reception on iOS. It is not an Android FCM transport implementation: confirmed. Source: https://docs.unity3d.com/Packages/com.unity.mobile.notifications@2.5/manual/index.html.
- notifications-03. Android notification channels are required when targeting Android 8.0, API level 26, or later; the user controls channel behavior after creation: confirmed. Source: https://developer.android.com/develop/ui/compose/notifications/channels.
- notifications-04. Create a channel before sending Android local notifications; read AndroidNotificationCenter.GetLastNotificationIntent, then Notification.IntentData, after a tap. The query returns null when the app opened another way: confirmed. Source: https://docs.unity3d.com/Packages/com.unity.mobile.notifications@2.5/manual/Android.html.
- notifications-05. Enable Push Notifications adds the Xcode push capability and is needed to retrieve DeviceToken from AuthorizationRequest; automatic launch-time notification authorization is optional: confirmed. Source: https://docs.unity3d.com/Packages/com.unity.mobile.notifications@2.5/manual/Settings.html.
- notifications-06. Mobile Notifications patches UNITY_USES_REMOTE_NOTIFICATIONS from 0 to 1 and adds the push capability with a configurable release APNs environment: confirmed. Source: https://cdn.packages.unity.com/tarballs/com.unity.mobile.notifications/2.5.0/6e5da11136c31d5b0abb4698f7a71396169789a8.tgz, released package `Editor/iOSNotificationPostProcessor.cs`, line 212.
- notifications-07. The shipped Trampoline defaults UNITY_USES_REMOTE_NOTIFICATIONS to 0: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/Preprocessor.h`, line 200.
- notifications-08. When remote notifications are enabled, UnityAppController posts the received NSData device token under kUnityDidRegisterForRemoteNotificationsWithDeviceToken: confirmed. Source: `PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, line 303.
- notifications-09. For iOS push registration, enable push support and create AuthorizationRequest with registerForRemoteNotifications true, then read DeviceToken. In 2.5.0 use QueryLastRespondedNotification and await completion to retrieve the notification or action that opened the app: confirmed. Source: https://docs.unity3d.com/Packages/com.unity.mobile.notifications@2.5/manual/iOS.html.
- notifications-10. FCM registration tokens can change after restore, reinstall or data clearing; onNewToken runs when a new token is generated. Send the current token to the backend for the current authenticated account: confirmed. Source: https://firebase.google.com/docs/cloud-messaging/android/get-started.
- notifications-11. In the foreground, FCM notification, data and combined messages reach onMessageReceived; in the background, data messages reach that callback while notification messages go to the system tray. Combined background messages deliver their data in launcher intent extras after the tap: confirmed. Source: https://firebase.google.com/docs/cloud-messaging/android/receive-messages.
- notifications-12. APNs identifies an app on a device with its device token; register at each launch and send the token to the provider with the user association. Tokens change after restore, a new device or operating-system reinstall: confirmed. Source: https://developer.apple.com/documentation/usernotifications/registering-your-app-with-apns.
- notifications-13. aps-environment selects development or production; Xcode normally derives it from the provisioning profile and the development environment is the sandbox. The Unity Development Build checkbox alone does not identify the signed APNs environment: narrowed to Inspect the signed entitlement and provisioning choice; project capability settings can be changed, so do not equate every development-style build with a sandbox token. Source: https://developer.apple.com/documentation/bundleresources/entitlements/aps-environment.
- notifications-14. APNs providers send development requests to api.sandbox.push.apple.com:443 and production requests to api.push.apple.com:443, using the device token and provider authentication: confirmed. Source: https://developer.apple.com/documentation/usernotifications/sending-notification-requests-to-apns.
- notifications-15. APNs reports BadDeviceToken when a token is invalid or does not match the environment; DeviceTokenNotForTopic is a different error: confirmed. Source: https://developer.apple.com/documentation/usernotifications/handling-notification-responses-from-apns.
- notifications-16. Apple says notification payloads should not contain customer information or sensitive data, and requires encryption if such data must be included. Treat reward-like payload values as hints requiring server authorization: narrowed to The ban on granting rewards from payloads and unlinking an account on logout are application security policies; neither is an automatic guarantee of push delivery. Source: https://developer.apple.com/documentation/usernotifications/generating-a-remote-notification.

- Package push changes: confirmed in released package source. `iOSNotificationPostProcessor` reads `USE_APS_RELEASE`, calls `ProjectCapabilityManager.AddPushNotifications(!useReleaseAPSEnv)`, patches `UNITY_USES_REMOTE_NOTIFICATIONS`, and adds the matching Swift define where appropriate. The capability creates the requested APNs entitlement; the signed environment still depends on signing and provisioning. The package’s `UnityNotificationLifeCycleManager.mm` observes the token notification on the main operation queue and passes the token to its notification manager. Source: https://cdn.packages.unity.com/tarballs/com.unity.mobile.notifications/2.5.0/6e5da11136c31d5b0abb4698f7a71396169789a8.tgz, `Editor/iOSNotificationPostProcessor.cs`, lines 26, 104, 114 and 209; `Runtime/iOS/Plugins/UnityNotificationLifeCycleManager.mm`, line 82; https://developer.apple.com/documentation/bundleresources/entitlements/aps-environment.
- iOS authorization and tap data: confirmed. Request only the needed Alert, Badge and Sound options; provisional authorization is a separate quiet trial option. Set the remote-registration flag when a device token is needed. In package 2.5.0, use the asynchronous `QueryLastRespondedNotification` operation; earlier last-notification getters are marked obsolete in package source. A notification action can also carry an action identifier or typed response. Source: https://docs.unity3d.com/Packages/com.unity.mobile.notifications@2.5/manual/iOS.html and https://developer.apple.com/documentation/usernotifications/asking-permission-to-use-notifications and the released package `Runtime/iOS/iOSNotificationCenter.cs`, lines 145 to 183.
- Account-token association and logout: narrowed to application policy. Upload the current registration for the authenticated account at the appropriate launch/refresh/change points, retry failed uploads, and remove or invalidate the old account association on logout. A token identifies an app installation or app-device pair, not permission to access a player account. Logout need not mean destroying the platform token itself. FCM explicitly documents restore, reinstall and data clearing as rotation cases; APNs documents restore, a new device and operating-system reinstall, and requires obtaining the current token rather than trusting a cached one. Source: https://firebase.google.com/docs/cloud-messaging/android/get-started and https://developer.apple.com/documentation/usernotifications/registering-your-app-with-apns. The unlink-on-logout rule is a security design recommendation inferred from account-associated delivery, not a quoted platform guarantee.


### os-auth-callbacks

- auth-01. Guideline 4.8 requires an equivalent login option with limited name/email collection, private email and no advertising interaction collection without consent when third-party/social login authenticates the primary account, subject to listed exceptions. It does not explicitly mandate Sign in with Apple by name: contradicted: Explain the current privacy criteria and all listed exception categories rather than the old universal Sign in with Apple rule. Source: https://developer.apple.com/app-store/review/guidelines/.
- auth-02. RFC 8252 requires an external user-agent and rejects embedded web views for OAuth authorization because the host can inspect credentials and session data: confirmed. Source: https://www.rfc-editor.org/rfc/rfc8252.html.
- auth-03. RFC 8252 prescribes code flow with PKCE and permits private-use, claimed HTTPS and loopback redirects, preferring claimed HTTPS where possible: confirmed. Source: https://www.rfc-editor.org/rfc/rfc8252.html.
- auth-04. A distributed native-app client secret is not confidential or proof of client identity: confirmed. Source: https://www.rfc-editor.org/rfc/rfc8252.html.
- auth-05. Match state and the expected redirect URI to a pending attempt; preserve it securely across process death or reject and restart: narrowed to Correlation is required; persistence and expiration are application design choices. Source: https://www.rfc-editor.org/rfc/rfc8252.html.
- auth-06. PKCE counters authorization-code interception by requiring the code verifier for redemption; use S256 when the client can support it: confirmed. Source: https://www.rfc-editor.org/rfc/rfc7636.html.
- auth-07. For S256, the challenge is unpadded base64url of SHA-256 over the ASCII verifier. Appendix B pairs verifier dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk with challenge E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM: confirmed. Source: https://www.rfc-editor.org/rfc/rfc7636.html.
- auth-08. State correlates request and callback; RFC 6749 requires CSRF protection for the redirect endpoint and describes a non-guessable binding value. State and PKCE address different checks and neither substitutes for backend authorization: confirmed. Source: https://www.rfc-editor.org/rfc/rfc6749.html.
- auth-09. Backend code exchange and session issuance are an architectural choice; RFC 8252 also supports native public-client redemption: narrowed to Backend exchange must preserve the verifier, client and redirect binding. Source: https://www.rfc-editor.org/rfc/rfc8252.html.
- auth-10. ASWebAuthenticationSession passes the authentication callback URL to its completion handler and isolates it to the initiating session. Do not require this callback to pass through Unity deepLinkActivated: contradicted: The browser callback mechanism depends on the API: an Apple authentication session completion is distinct from ordinary app/scene URL delivery. Source: https://developer.apple.com/documentation/authenticationservices/aswebauthenticationsession.
- auth-11. ASWebAuthenticationSession reports canceledLogin when the user cancels: confirmed. Source: https://developer.apple.com/documentation/authenticationservices/aswebauthenticationsessionerror/code/canceledlogin.
- auth-12. ASWebAuthenticationSession supports HTTPS callbacks matched by host and path; the host must belong to an associated domain: confirmed. Source: https://developer.apple.com/documentation/authenticationservices/aswebauthenticationsession/callback/https(host:path:).
- auth-13. Android Custom Tabs use the browser rather than an app-owned embedded web view, sharing browser features and session state. Ordinary Custom Tabs authentication redirects return through an app intent: confirmed. Source: https://developer.chrome.com/docs/android/custom-tabs.
- auth-14. A closed authentication browser does not universally mean no callback: Auth Tab returns cancellation results, including RESULT_CANCELED for its Custom Tabs fallback. Use supported cancellation signals plus expiration of unresolved attempts: contradicted: Ordinary redirects, Custom Tabs session/visibility events and Auth Tab activity results are distinct mechanisms; a hidden tab alone is not proof of OAuth success or cancellation. Source: https://developer.chrome.com/docs/android/custom-tabs/guide-auth-tab.
- auth-15. Android’s developer option that destroys activities tests activity recreation, not whole-process death. Also test killing the cached process while the authorization browser is foregrounded: narrowed to A process-death test must confirm a new PID and clean recovery or rejection of the pending response; retained static state cannot satisfy it. Source: https://developer.android.com/studio/debug/dev-options.
- auth-16. Android Keystore holds cryptographic keys with non-exportable key material; use such keys to protect stored attempt or session data, rather than describing Keystore as a generic string vault: confirmed. Source: https://developer.android.com/privacy-and-security/keystore.
- auth-17. The iOS Keychain stores small secrets in an encrypted database; it can hold a pending verifier or session credential with suitable accessibility rules: confirmed. Source: https://developer.apple.com/documentation/security/keychain-services.

- Current Login Services exceptions: confirmed. Guideline 4.8 exempts an app using only its own account system; a marketplace-specific login in an alternative marketplace context; an education, enterprise or business app requiring an existing institutional account; a government or industry-backed citizen identity system; and a client that signs directly into a specific third-party service to access that service’s content. The nonexempt alternative must minimize name/email collection, allow private email, and avoid collecting app interactions for advertising without consent. Sign in with Apple is an available platform service, but the present clause does not require it by name. Source: https://developer.apple.com/app-store/review/guidelines/, section 4.8.
- OAuth boundaries: confirmed or narrowed as indicated above. RFC 8252 sections 6 and 7 describe the code flow, PKCE and three redirect options; sections 8.5, 8.9, 8.10 and 8.12 cover distributed secrets, state, redirect matching and embedded user-agents. RFC 7636 section 4.2 defines S256; Appendix B provides verifier `dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk` and challenge `E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM`. RFC 6749 sections 4.1.1 and 10.12 explain state and CSRF. These standards do not prescribe the book’s particular backend session architecture. Source: https://www.rfc-editor.org/rfc/rfc8252.html and https://www.rfc-editor.org/rfc/rfc7636.html and https://www.rfc-editor.org/rfc/rfc6749.html.
- Browser return and cancellation: narrowed by API. A normal Custom Tabs redirect reaches the app through an intent. `CustomTabsCallback.TAB_HIDDEN` only says that a tab became hidden; the engagement API can report session end. Auth Tab can return a callback URI and cancellation results, including cancellation from its fallback flow. Apple’s `ASWebAuthenticationSession` delivers its own completion and canceled-login error rather than requiring app URL delegate dispatch. Correlate all successful results, tolerate lost processes, and expire unresolved attempts; do not treat resume or a hidden tab as success. Source: https://developer.chrome.com/docs/android/custom-tabs/guide-auth-tab and https://developer.android.com/reference/androidx/browser/customtabs/CustomTabsCallback and https://developer.chrome.com/docs/android/custom-tabs/guide-engagement-signals and https://developer.apple.com/documentation/authenticationservices/aswebauthenticationsession.
- Process-death recovery: narrowed to a design requirement grounded in the lifecycle and RFC contracts. When the browser is foregrounded, the app process may become cached and be killed. Securely retaining the pending state, verifier, redirect URI, issuer and expiration permits correlation after restart; otherwise reject and restart the flow. Keep the provider code and the resulting backend session separate. The developer setting that destroys activities does not prove a process-death recovery path. Source: https://developer.android.com/guide/components/activities/process-lifecycle and https://developer.android.com/studio/debug/dev-options and RFC 8252 sections 8.9 and 8.10.


## Rests on documentation alone

- Android process killability, permission prompt behavior, App Links verification, FCM delivery routing and platform secure-storage contracts. The Java exports and bytecode support only the Unity-side paths stated above.
- Apple lifecycle budgets, suspension, permission behavior, Universal Links, APNs environments and response reasons, authentication-session contracts and App Review policy. Purpose-string termination and APNs error behavior were not reproduced in this pass.
- Unity’s documented managed callbacks, background setting and one-frame native-message delay, on Android. On iOS the run under **Runs** recorded the callback order and a message held while paused; Android has no device here, so its order and its paused-message behavior rest on the sources.
- OAuth requirements and the PKCE example are grounded in the RFCs. Backend exchange, account unlinking on logout, authorization of reward claims, routing readiness and duplicate handling remain explicitly identified application design decisions.

## Narrowed or cut

- Cut the claim that Unity passes null saved state or discards every Android saved-state facility. Both exports forward the Bundle to their superclass; automatic restoration of Unity gameplay was not found.
- Do not rely on a final quit callback, a guaranteed analytics flush, a fixed focus/pause ordering, universal socket loss or a burst of all queued native messages after resume.
- Replace the old iOS background dropdown with the current documented checkbox. Do not apply the visionOS-only background focus/pause block to iOS.
- Retain `PermissionDeniedAndDontAskAgain` as an existing but obsolete and unreliable API. Use denial plus current status/rationale instead of promising a permanent-denial callback.
- Replace blanket startup-permission and universal one-time-prompt statements with feature-specific behavior. The current ATT documentation contains a conditional annual re-request rule for the European Union.
- Do not equate Unity Development Build with the signed APNs environment. Use the actual entitlement, and name `BadDeviceToken` precisely.
- Do not claim every authentication result enters through a deep link or that browser cancellation never produces a callback.
- Replace the old universal Sign in with Apple mandate with the current equivalent-option criteria and exception list.
- Treat HTTPS association as handler verification, not sender authentication or authorization to grant anything.

## Where the outline fell short

- Unity 6000.3.11f1 already ships a scene manifest. Apple delivers cold and warm URL inputs through scene APIs, and with scenes the launch options are nil, while `UnityScene` has only lifecycle forwarding. The run under **Runs** showed custom-scheme links lost cold and warm, and a `UnityScene` category restoring both; the outline assumed the `UnityAppController` paths still receive links. Chapter 01 says Unity stores the launching link in `absoluteURL`, which does not hold on iOS in 6000.3.11f1 without such a category; changing its committed question needs the user's consent.
- Native call order and managed callback order are different evidence questions. The sources expose guards for engine readiness, custom background behavior, snapshots, multi-window mode and independent window focus delivery.
- `Application.memoryUsageChanged` exists in the reference version, and Android permission dismissal is a distinct callback. The permanent-denial event carries an obsolete attribute that the outline did not account for.
- The released Mobile Notifications version is 2.5.0, and its iOS tap query is asynchronous. Notification registration, notification authorization, channel settings, the device token and the authenticated player association are separate pieces of state.
- The app-based five-state explanation is a conceptual model. Scene activation has its own states and must not be presented as the same enum; a scene can disconnect without the whole process terminating.
- A canceled external authentication flow, lost pending attempt and expired attempt require explicit outcomes. Android Keystore protects keys for encryption; it is not interchangeable with the iOS Keychain’s small-secret storage model.

## Checked while writing

Claims the chapter makes beyond Codex's receipts, each checked against its source on 2026-09-25:

- Unity pauses the player when an iOS app resigns active, not when it enters the background: `applicationWillResignActive:` calls `UnitySetPlayerFocus(0)` and then `UnityPause(1)` unless `UnityGetUseCustomAppBackgroundBehavior()` (`PlaybackEngines/iOSSupport/Trampoline/Classes/UnityAppController.mm`, lines 607 to 650). UIKit resigns an app “for temporary interruptions, such as when displaying system alerts” (https://developer.apple.com/documentation/uikit/uiscenedelegate/scenewillresignactive(_:)) and for “an incoming phone call or SMS message” (https://developer.apple.com/documentation/uikit/uiapplicationdelegate/applicationwillresignactive(_:)). The chapter's claim that a permission prompt pauses the game on iOS rests on these two, and was not run.
- Unity requests location, camera and microphone permissions “the first time that the application uses” `LocationService`, `WebCamTexture` or `Microphone` (https://docs.unity3d.com/6000.3/Documentation/Manual/android-permissions-in-unity.html).
- After the player taps Deny more than once, “the user will no longer see the system permissions dialog” (https://developer.android.com/training/permissions/requesting).
- A category method that clashes with the class's own is undefined “as to which method implementation is used at runtime” (Apple's “Customizing Existing Classes” in the Objective-C guide, https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ProgrammingWithObjectiveC/CustomizingExistingClasses/CustomizingExistingClasses.html).
- `adb shell am kill <package>` kills a package's processes that are safe to kill (https://developer.android.com/tools/adb).
- FCM's token changes (restored on a new device, reinstalled, app data cleared), from https://firebase.google.com/docs/cloud-messaging/android/get-started; APNs registration “each time your app launches” and “Never cache device tokens in local storage”, from https://developer.apple.com/documentation/usernotifications/registering-your-app-with-apns.
- Play App Signing: a fingerprint computed locally “will usually not match the one on users' devices”, and Play Console's App signing page has “the correct Digital Asset Links JSON snippet” (https://developer.android.com/training/app-links/configure-assetlinks). Unverified web links on Android 12 and later “will generally open in a web browser by default” (https://developer.android.com/training/app-links/create-deeplinks).
- Guideline 4.8's three criteria, word for word from https://developer.apple.com/app-store/review/guidelines/; RFC 8252's reverse-domain rule for private-use schemes and RFC 7636's Appendix B challenge.
- Code: the four C# samples compile against the 6000.3.11f1 `UnityEngine.CoreModule.dll` and `UnityEngine.AndroidJNIModule.dll` with `UNITY_ANDROID` defined and warnings as errors, extracted from the chapter file. `GameLinks.TryParse` and `PkceAttempt` run the same on .NET 8 and on the Editor's Mono: three valid links accepted and six rejected (an unknown route, another host, a path with extra segments, a lowercase id, plain http, not a URL), and the Appendix B challenge reproduced. The `UnityScene` category is the probe's `Assets/Plugins/iOS/Disabled~/UnitySceneLinks.mm`, byte for byte; it compiles for the Simulator SDK and ran as **Runs** describes.

## Teacher's read

Codex (GPT-6 Astra, `xhigh`, read-only) read the chapter blind against this file and the receipts, and returned eight problems. Each was checked and each held; the chapter was changed as follows.

- A missing purpose string ends the app for camera and microphone capture, while a location request without `NSLocationWhenInUseUsageDescription` “will do nothing” (`CLLocationManager.h` in the iOS 27.0 SDK, lines 484 to 486). The section's opening now says so.
- “Development builds” in an APNs question read as Unity's Development Build checkbox, which the next variant says has no say; the prompt now names a development provisioning profile and the option the `aps-environment` value.
- A suspended app's own requests stop, but a background `URLSession` transfer continues “even when the app itself is suspended or terminated” (`NSURLSession.h`, lines 1284 to 1285); the lifecycle section now names the exception.
- A closed Custom Tab returns no sign-in result, but a registered callback can report `TAB_HIDDEN`, and the engagement signals end with `onSessionEnded` (Chrome's Custom Tabs reference and engagement guide); the prose and the variant no longer say “nothing”.
- That variant tested cancellation under the process-death concept. Rather than add an eleventh concept against the outline's figures, it now asks what ends a stored attempt when the tab is closed, which is the attempt's lifetime.
- “iOS asks once” was scoped to notifications and location (the header says a location request “will do nothing” after the first answer), and App Tracking Transparency's European rule of a new request after a year is stated with its date.
- “Neither platform tells the game who sent a link” was false: iOS can pass the opening app in the options dictionary, which `UnityAppController.mm` forwards to listeners. The explanation now says that nothing in a link proves who wrote it.
- `[[Info.plist]]` and `[[scene delegate]]` were not linked at their first mention in two sections, nor the push capability in the notifications section.

## Runs

One batched run on 2026-09-25, the one this phase needed: the iOS link question that Phase 21 left open. Unity 6000.3.11f1, the probe project's Simulator export (`ProbeBuild.ExportIosSimulator`, then `AppendIosSimulator`), built with `xcodebuild` (Debug, unsigned) and run in the iOS 27.0 Simulator on an iPhone 17 Pro. The driver `Assets/Scripts/IosProbe/IosProbe.cs` subscribes to `Application.deepLinkActivated` from a `RuntimeInitializeOnLoadMethod`, logs `Application.absoluteURL` there and whenever it changes, and logs `OnApplicationFocus` and `OnApplicationPause`; `Assets/Plugins/iOS/Probe/ProbeListener.mm` logs the listener's `onOpenURL:` and lifecycle notifications, and sends a `UnitySendMessage` from `didEnterBackground:`. The C# lines go to `Documents/probe.log` in the app's container, since an app that iOS launches for a link has no console.

- How the links were sent: `xcrun simctl openurl` stops at a confirmation, and so does one app opening another's custom scheme. A helper app that opens the URL it is launched with raised the confirmation “‘LinkOpener’ wants to open ‘PlatformLayerProbe’” with Cancel and Open. A UI test (XCUITest) launches the helper and taps Open in SpringBoard. iOS asked once for that pair of apps and opened later links from the same helper without asking.
- Stock export, cold custom-scheme link: the app launched for the link. `absoluteURL` was empty at the first `RuntimeInitializeOnLoadMethod` and stayed empty; no `deepLinkActivated`; no `onOpenURL:`. The link was lost. Run twice.
- Stock export, warm (the app running, sent to the background by the helper): the app came back to the front; `absoluteURL` stayed empty; no `deepLinkActivated`; no `onOpenURL:`. Lost.
- With the fix, `Assets/Plugins/iOS/Disabled~/UnitySceneLinks.mm` in the probe (a category on `UnityScene` that adds `scene:willConnectToSession:options:`, `scene:openURLContexts:` and `scene:continueUserActivity:` and hands each link to `UnityAppController`), moved into `Plugins/iOS/Probe/` for an Append build. Cold: `absoluteURL` held the link at the first `RuntimeInitializeOnLoadMethod`, the listener's `onOpenURL:` fired, and `deepLinkActivated` was not raised, which is Unity's documented split between the launch link and later ones. Warm: `deepLinkActivated` fired with the link, `absoluteURL` updated, and `onOpenURL:` fired. The link arrived before `willEnterForeground:`, and `deepLinkActivated` ran 0.4 s before `OnApplicationFocus(True)` and `OnApplicationPause(False)`, while the player was still paused: unlike `UnitySendMessage`, the event is not held for the next frame.
- Lifecycle order on iOS, from the same runs. Launch: `OnApplicationPause(False)` then `OnApplicationFocus(True)`, both at frame 0. Leaving for another app: `OnApplicationFocus(False)` then `OnApplicationPause(True)`, both at the same frame and both within 11 ms of each other at resign-active time; the listener's `didEnterBackground:` came 2.4 s later. Returning: `OnApplicationFocus(True)`, then the message sent from `didEnterBackground:`, then `OnApplicationPause(False)`, on the first frame after resume.
- `UnitySendMessage` while paused: the message sent from `didEnterBackground:`, after the player had paused, ran on the first frame after the app returned, before `OnApplicationPause(False)`. It was held, not dropped.
- Not run: universal links, which need the Associated Domains entitlement and so a signed build; the category's `continueUserActivity` path compiles and hands the activity to the controller method that the Trampoline already implements. Android, since no emulator or device is present; its order and its link path rest on the shipped sources.

